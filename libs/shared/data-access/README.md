# @shiftsync/data-access

This is the shared library I built as the backbone of ShiftSync. It serves as the single source of truth for database schemas, entity types, DTO validation, and repository logic — consumed by both the NestJS backend and the Next.js frontend.

## Running unit tests

Run `nx test data-access` to execute the unit tests via [Jest](https://jestjs.io).
