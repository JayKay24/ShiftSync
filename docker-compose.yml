services:
  db:
    image: postgres:16-alpine
    container_name: shiftsync_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: shiftsync
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d shiftsync"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  server:
    build:
      context: .
      dockerfile: Dockerfile-server
    container_name: shiftsync_server
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@db:5432/shiftsync?sslmode=disable
      - PORT=3000
    depends_on:
      db:
        condition: service_healthy
    restart: always

  client:
    build:
      context: .
      dockerfile: Dockerfile-client
    container_name: shiftsync_client
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
    depends_on:
      - server
    restart: always

volumes:
  postgres_data:
